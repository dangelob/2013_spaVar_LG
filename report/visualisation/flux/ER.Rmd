---
title: "ER"
author: "Benoît D'ANGELO"
date: "23/02/2015"
output: html_document
---

```{r, echo=FALSE, message=FALSE}
library(laguettevarspa)
library(dplyr)
library(tidyr)
library(ggplot2)
```


```{r, echo=FALSE}
df <- svNetFlux %>%
  filter(type == "ER")%>%
  separate(date, c("year","month", "day"),remove = F) %>%
  separate(timestamp, c("ts_date", "time"), sep=" ") %>%
  mutate(time = substr(time, 1, 5))

ymER <- df %>%
  group_by(year) %>%
  summarise(ER = mean(netCO2F, na.rm=T), ER_sd = sd(netCO2F, na.rm=T))
A <- filter(df, year==2013)
B <- filter(df, year==2014)
t.test(A$netCO2F, B$netCO2F)

mmER <- df %>%
  group_by(month) %>%
  summarise(ER = mean(netCO2F, na.rm=T), ER_sd = sd(netCO2F, na.rm=T))

mymER <- df %>%
  group_by(year, month) %>%
  summarise(ER = mean(netCO2F, na.rm=T), ER_sd = sd(netCO2F, na.rm=T))

```

```{r, echo=FALSE}
nobs <- paste("N :", as.character(NROW(df)))

df$colR2 <- ifelse(df$R2 >= 0.8, "good", "bad")

ggplot(df, aes(x=date, y=netCO2F, color = placette))+
  geom_point()+
  annotate("text", x=as.Date("2013-06-01"), y=15, label=nobs, size=3.5)+
#   facet_grid(.~year, scales = "free")+
  theme_bw()

ggplot(df, aes(x=date, y=netCO2F))+
  geom_line(aes(group = year))+
  geom_point(aes(color=colR2))+
  scale_colour_manual(values=c("red","black"))+
  geom_text(data=df, aes(x=date, y=netCO2F+4, label=time), size=2.5, angle=90)+
#   annotate("text", x=df$date, y=df$netCO2F+1, label=df$time, size=3.5)+
  facet_wrap(~placette)+
  theme_bw()+
  theme(legend.position="none")
```

```{r, echo=FALSE}
ggplot(ymER, aes(x=year, y=ER))+
  geom_point()+
  geom_line(aes(x=year, y=ER+ER_sd, group=1))+
  geom_line(aes(x=year, y=ER-ER_sd, group=1))+
  theme_bw()

ggplot(mmER, aes(x=month, y=ER))+
  geom_point()+
  geom_line(aes(x=month, y=ER+ER_sd, group=1))+
  geom_line(aes(x=month, y=ER-ER_sd, group=1))+
  theme_bw()

ggplot(mymER, aes(x=month, y=ER, color=year))+
  geom_point()+
  geom_line(aes(x=month, y=ER+ER_sd, group=year))+
  geom_line(aes(x=month, y=ER-ER_sd, group=year))+
  theme_bw()

ggplot(mymER, aes(x=month, y=ER_sd, color=year))+
  geom_point()+
#   geom_line(aes(x=month, y=ER+ER_sd, group=year))+
#   geom_line(aes(x=month, y=ER-ER_sd, group=year))+
  theme_bw()
```
